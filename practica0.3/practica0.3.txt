En esta practica se programo un algoritmo para encontrar el maximo de un conjunto de numeros. E n el lenguaje ensamblador es muy importante especificar cada paso de un algoritmo, asi el ordenador sabra como identificar el maximo. Mediante una serie de modificaciones que se le hicieron al progrma principal "maximo", se produjeron otrs programas como econtrar ahora el minimo, cambiar el numero con el que el programa identificaba el final de la lista de numeros. Solo basto con modificar y reacomodar ciertas directivas. 
